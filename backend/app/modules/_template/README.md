# Backend Module Template

This directory is documentation-only. Files use `.template` extensions so the package is not importable or registered.

Copy these templates when creating a new module, then rename files inside the new module package. Do not register `_template`.
