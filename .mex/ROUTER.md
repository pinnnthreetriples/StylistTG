---
name: StylistTG memory router
description: Session bootstrap and routing table for project memory.
edges:
  - .mex/AGENTS.md
  - .mex/status/current.md
  - .mex/context/architecture.md
  - .mex/context/backend.md
  - .mex/context/frontend.md
  - .mex/context/workers.md
  - .mex/context/warmup.md
  - .mex/context/warmup-advanced-state.md
  - .mex/context/security.md
  - .mex/context/setup.md
  - .mex/context/stack.md
  - .mex/context/deploy.md
  - .mex/context/conventions.md
  - .mex/context/decisions.md
  - .mex/patterns/INDEX.md
  - .mex/patterns/architecture-change.md
last_updated: 2026-06-09
---

# StylistTG Memory Router

## Load Policy

Always read:

1. `AGENTS.md`
2. `.mex/ROUTER.md`
3. `.mex/status/current.md`

Then read only the routed context and pattern for the task.

Do not read `AGENT_HANDOFF.md` by default. Use the archived handoff only as a historical fallback when `.mex` and `docs/` do not answer the question.

## Current Project State

### Working

- React/TypeScript/Vite dashboard lives in `apps/dashboard` with shared packages under `packages/`.
- FastAPI backend lives in `backend/app` with routers, shared services, adapters, RQ workers, SQLAlchemy models, and module-owned boundaries under `backend/app/modules/`.
- New feature behavior should be implemented through module-owned code; legacy API/service/worker paths remain compatibility wrappers unless explicitly migrated.
- Local dashboard launcher `scripts/start-dev.ps1` starts Memurai Redis, backend on port `8002`, profile/auth workers, and Vite on `5173`.
- Queue taxonomy includes `auth_jobs`, `profile_jobs`, `media_jobs`, `story_jobs`, `account_lifecycle_jobs`, `maintenance_jobs`, `scheduler_jobs`, `warmup_jobs`, `warmup_dispatch_jobs`, and `neuro_comment_jobs`.
- Account Preparation / Warmup is implemented at backend `/api/warmup` and frontend `/modules/warmup` through module-owned code plus compatibility wrappers.
- Frontend is polling-first; no WebSocket/SSE contract exists.
- PostgreSQL is the persisted source of truth; Redis/RQ is execution infrastructure.
- Runtime policy toggles changed through APIs are persisted in PostgreSQL, not process-local settings.

### Limited or gated

- Workspace Safety Policy is temporarily neutralized by `WORKSPACE_SAFETY_POLICY_TEMPORARILY_DISABLED=True`; see `.mex/status/current.md` and `docs/runbooks/safety-rollout.md`.
- Live TDLib execution is disabled by default and must remain explicitly gated.
- Live warmup modes require `WARMUP_LIVE_ENABLED` plus mode-specific gates and operator approval.
- `scripts/start_backend.ps1` and live-validation helpers may use port `8000`; dashboard dev uses `8002`.
- API embedded stale-job reaper is disabled by default; production should run scheduler/reaper work outside API replicas.
- Historical handoff content is archived under `docs/archive/`; `.mex` is the structured entrypoint.

### Known issues / watch items

- `.env.example` points dashboard dev at backend `8002`; live-validation helpers may still use `8000` and should use `$ApiBaseUrl` from startup output.
- `node_modules`, build outputs, logs, artifacts, and caches may be absent after cleanup and regenerated by install/build/test commands.
- Never inspect or copy `.env*`, `backend/tdlib/`, logs, or artifacts unless the user explicitly approves the specific action.

## Routing Table

| Task type | Load first | Then check |
| --- | --- | --- |
| Architecture, data flow, or new module | `.mex/context/architecture.md` | `.mex/patterns/architecture-change.md`, `.mex/context/decisions.md`, `docs/architecture/AGENT_ARCHITECTURE_GUIDE.md`, `docs/architecture/STRUCTURE_AUDIT.md` |
| Backend API/service change | `.mex/context/backend.md` | `.mex/patterns/backend-api-change.md` |
| Frontend module/UI change | `.mex/context/frontend.md` | `.mex/patterns/frontend-module-change.md`, `.agents/context/PRODUCT.md`, `.agents/context/DESIGN.md` |
| Worker/queue/scheduler change | `.mex/context/workers.md` | `.mex/patterns/worker-queue-change.md` |
| Warmup/account-preparation | `.mex/context/warmup.md` | `.mex/patterns/warmup-change.md`, `docs/runbooks/account-preparation.md` |
| Advanced warmup roadmap | `.mex/context/warmup-advanced-state.md` | `.mex/patterns/warmup-advanced.md`, `docs/design/warmup-advanced-file-map.md` |
| Security/live TDLib/env | `.mex/context/security.md` | `.mex/status/current.md`, `.mex/patterns/live-tdlib-safety.md` |
| Setup/local runtime | `.mex/context/setup.md` | `.mex/context/stack.md`, `README.md` |
| Deploy/staging/cloud | `.mex/context/deploy.md` | Relevant runbook in `docs/runbooks/` |
| Coding standards/review | `.mex/context/conventions.md` | Relevant tests and lint/typecheck commands |
| Documentation audit | `.mex/patterns/documentation-audit.md` | `docs/`, `README.md`, `.mex/status/current.md` |
| Safe cleanup | `.mex/patterns/safe-cleanup.md` | `.gitignore`, `git clean -ndX` only |
| Memory/scaffold update | `.mex/patterns/mex-memory-update.md` | `.mex/context/decisions.md` |
| Board/issue/PR work | `docs/agents/project-board.md` | `.mex/patterns/board-workflow.md` |

Always check `.mex/patterns/INDEX.md` for a matching task pattern before editing.

## Behavioural Contract

1. CONTEXT: load the routed context files and matching pattern before non-trivial work.
2. BUILD: make focused changes; state any deviation from the pattern before editing.
3. VERIFY: run the smallest useful checks and report pass/fail precisely.
4. DEBUG: if verification fails, inspect root cause and re-run targeted checks.
5. GROW: update `.mex` context/patterns only when the task changes stable project knowledge; skip memory edits for small code edits, typo fixes, test-only changes, local debug steps, and purely visual tweaks.

## Source-of-truth rules

- `AGENTS.md` is the canonical cross-agent startup anchor.
- `CLAUDE.md` imports `AGENTS.md` and carries Claude Code-specific notes only.
- `.mex/AGENTS.md` is a mex compatibility pointer.
- `.mex` stores compact memory and task patterns, not raw secrets, logs, TDLib sessions, or full generated artifacts.
- Temporary rollout/runtime state belongs in `.mex/status/current.md` with a review date.
- Detailed canonical documentation remains in `docs/`; `.mex` should link or summarize instead of duplicating large runbooks.
- Decisions are append-only in `.mex/context/decisions.md`; mark superseded decisions instead of deleting history.
- Architecture and new-module work must follow `.mex/patterns/architecture-change.md` and `docs/architecture/AGENT_ARCHITECTURE_GUIDE.md`.
- Run `npm run memory:check` after memory/docs/scaffold changes or when commands, paths, ports, routes, queues, feature flags, or architecture change.
- Do not run `npm run memory:sync` without explicit user approval; prefer `npm run memory:sync:dry-run`.
