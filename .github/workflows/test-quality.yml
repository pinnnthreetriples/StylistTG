name: Test Quality

on:
  pull_request:
    paths:
      - "backend/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/test-quality.yml"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  PYTHON_VERSION: "3.12"
  NODE_VERSION: "20"
  UV_VERSION: "0.10.9"

jobs:
  lint-format:
    name: lint-format
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - name: Install locked backend dependencies
        run: |
          python -m pip install uv==${{ env.UV_VERSION }}
          uv sync --locked --extra dev
      - name: Require tests for new backend production files
        run: uv run python scripts/require_tests_for_new_backend_files.py --base origin/${{ github.base_ref }}
      - name: Ruff lint
        run: uv run ruff check . --output-format=github
      - name: Ruff format check
        run: uv run ruff format --check .

  typecheck:
    name: typecheck
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - name: Install locked backend dependencies
        run: |
          python -m pip install uv==${{ env.UV_VERSION }}
          uv sync --locked --extra dev
      - name: Pyright scoped type check
        run: uv run pyright app/api app/services app/schemas.py app/config.py app/workers
      - name: Pyright full strict report
        run: uv run pyright

  backend-tests:
    name: backend-tests
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - name: Install locked backend dependencies
        run: |
          python -m pip install uv==${{ env.UV_VERSION }}
          uv sync --locked --extra dev
      - name: Prepare reports directory
        run: mkdir -p reports
      - name: Run tests with coverage
        run: |
          uv run pytest tests \
            -n auto --dist=loadscope \
            --cov=app --cov=tools --cov-branch --cov-context=test \
            --cov-report=json:reports/coverage.json \
            --cov-report=xml:reports/coverage.xml \
            --cov-report=term-missing:skip-covered \
            --durations=20 --durations-min=1.0 \
            -q
        env:
          APP_ENV: test
          AUTH_MODE: local
      - name: Per-package coverage gate
        run: uv run python scripts/coverage_gate.py
      - name: Run test quality analyzer SARIF
        run: |
          uv run python -m tools.test_analyzer \
            --path tests \
            --coverage reports/coverage.json \
            --format sarif \
            --output reports/test-quality.sarif \
            --severity INFO
      - name: Run test quality analyzer JSON
        run: |
          uv run python -m tools.test_analyzer \
            --path tests \
            --coverage reports/coverage.json \
            --format json \
            --output reports/test-quality.json \
            --severity INFO
        if: always()
        continue-on-error: true
      - name: Upload backend test reports
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: backend-test-quality-reports
          path: |
            backend/reports/coverage.json
            backend/reports/coverage.xml
            backend/reports/test-quality.sarif
            backend/reports/test-quality.json
        if: always()

  audit:
    name: audit
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - name: Install locked backend dependencies
        run: |
          python -m pip install uv==${{ env.UV_VERSION }}
          uv sync --locked --extra dev
      - name: pip-audit
        run: uv run python -m pip_audit --skip-editable --progress-spinner=off

  duplication:
    name: duplication
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
      - name: Install Node quality dependencies
        run: npm ci --ignore-scripts
      - name: Prepare reports directory
        run: mkdir -p backend/reports
      - name: jscpd duplicate detection app
        run: npm exec -- jscpd backend/app --gitignore --noTips --threshold 2 --reporters json --output backend/reports/jscpd-app
      - name: jscpd duplicate detection tests
        run: npm exec -- jscpd backend/tests --gitignore --noTips --threshold 5 --reporters json --output backend/reports/jscpd-tests
      - name: Upload duplication reports
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: duplication-reports
          path: |
            backend/reports/jscpd-app/jscpd-report.json
            backend/reports/jscpd-tests/jscpd-report.json
        if: always()

  contract-fuzz:
    name: contract-fuzz
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
      - name: Install locked backend dependencies
        run: |
          python -m pip install uv==${{ env.UV_VERSION }}
          uv sync --locked --extra dev
      - name: Prepare reports directory
        run: mkdir -p reports
      - name: Schemathesis OpenAPI fuzz soft
        run: |
          uv run pytest tests/contract/ -m contract \
            -p no:randomly \
            --tb=short -q \
            --junit-xml=reports/schemathesis-junit.xml \
            || true
        env:
          APP_ENV: test
          AUTH_MODE: local
      - name: Upload contract fuzz report
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: contract-fuzz-reports
          path: backend/reports/schemathesis-junit.xml
        if: always()

  test-quality-pr:
    name: test-quality-pr
    if: always() && github.event_name == 'pull_request'
    needs:
      - lint-format
      - typecheck
      - backend-tests
      - audit
      - duplication
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Assert required quality jobs passed
        run: |
          echo "lint-format:   ${{ needs.lint-format.result }}"
          echo "typecheck:     ${{ needs.typecheck.result }}"
          echo "backend-tests: ${{ needs.backend-tests.result }}"
          echo "audit:         ${{ needs.audit.result }}"
          echo "duplication:   ${{ needs.duplication.result }}"
          test "${{ needs.lint-format.result }}" = "success"
          test "${{ needs.typecheck.result }}" = "success"
          test "${{ needs.backend-tests.result }}" = "success"
          test "${{ needs.audit.result }}" = "success"
          test "${{ needs.duplication.result }}" = "success"
