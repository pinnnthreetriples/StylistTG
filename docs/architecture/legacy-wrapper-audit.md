# Legacy Wrapper Audit

Legacy wrappers remain for import compatibility while canonical ownership moves
to `app.modules`. Do not add behavior to these files.

| Legacy path | Canonical owner | Current reason to keep | Allowed imports | Forbidden changes | Planned removal condition | Known call-sites |
| --- | --- | --- | --- | --- | --- | --- |
| `app.api.account_lifecycle_routes` | `app.modules.account_lifecycle.router` | Preserves legacy account lifecycle router import path. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.api.account_safety_routes` | `app.modules.account_safety.accounts_router` | Preserves legacy account-safety accounts router import path. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.api.account_update` | `app.modules.account_editing.router` | Preserves legacy router import path for /api/account-update. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.api.neuro_commenting` | `app.modules.neuro_commenting.router` | Preserves legacy router import path for /api/neuro-commenting. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.api.safety_policy` | `app.modules.account_safety.policy_router` | Preserves legacy workspace safety policy router import path. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.api.warmup` | `app.modules.warmup.router` | Preserves legacy router import path for /api/warmup. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.contracts.safety` | `app.modules.account_safety.read_contracts` | Preserves old account safety read contract imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.contracts.safety_gate` | `app.modules.account_safety.gate_contracts` | Preserves old account safety gate contract imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.account_batch_safety` | `app.modules.account_safety.batch_preview` | Preserves old account batch safety preview service imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.account_lifecycle` | `app.modules.account_lifecycle.service` | Preserves old account lifecycle service imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.account_safety` | `app.modules.account_safety.read_models` | Preserves old account safety read model service imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.account_safety_gate` | `app.modules.account_safety.gate` | Preserves old account safety gate service imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.account_safety_overrides` | `app.modules.account_safety.overrides` | Preserves old account safety override service imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.account_update_jobs` | `app.modules.account_editing.service` | Preserves old account update preview/job service imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.account_update_plan` | `app.modules.account_editing.planner` | Preserves old account update planner imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.auth_context` | `app.modules.auth.dependencies / context` | Preserves existing auth dependency imports and override keys. | `tests, external_compatibility, backend/app/api` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting` | `app.modules.neuro_commenting` | Preserves old neuro-commenting package imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.account_health_service` | `app.modules.neuro_commenting.account_health_service` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.account_selector` | `app.modules.neuro_commenting.account_selector` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.ai_comment_generator` | `app.modules.neuro_commenting.ai_comment_generator` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.ai_provider_openai` | `app.modules.neuro_commenting.ai_provider_openai` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.analytics_service` | `app.modules.neuro_commenting.analytics_service` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.approval_expirer` | `app.modules.neuro_commenting.approval_expirer` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.approval_service` | `app.modules.neuro_commenting.approval_service` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.campaign_account_service` | `app.modules.neuro_commenting.campaign_account_service` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.campaign_service` | `app.modules.neuro_commenting.campaign_service` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.channel_rules_service` | `app.modules.neuro_commenting.channel_rules_service` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.discussion_resolver` | `app.modules.neuro_commenting.discussion_resolver` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.enums` | `app.modules.neuro_commenting.enums` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.errors` | `app.modules.neuro_commenting.errors` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.jobs` | `app.modules.neuro_commenting.job_handlers` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.limits_service` | `app.modules.neuro_commenting.limits_service` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.live_readiness_service` | `app.modules.neuro_commenting.live_readiness_service` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.post_context_builder` | `app.modules.neuro_commenting.post_context_builder` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.post_detector` | `app.modules.neuro_commenting.post_detector` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.prompt_builder` | `app.modules.neuro_commenting.prompt_builder` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.prompt_presets` | `app.modules.neuro_commenting.prompt_presets` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.rate_limiter` | `app.modules.neuro_commenting.rate_limiter` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.repository` | `app.modules.neuro_commenting.repository` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.rules_policy` | `app.modules.neuro_commenting.rules_policy` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.safety_policy` | `app.modules.neuro_commenting.safety_policy` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.sender_service` | `app.modules.neuro_commenting.sender_service` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.target_health_service` | `app.modules.neuro_commenting.target_health_service` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.target_service` | `app.modules.neuro_commenting.target_service` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.tdlib_comment_sender` | `app.modules.neuro_commenting.tdlib_comment_sender` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.tdlib_helpers` | `app.modules.neuro_commenting.tdlib_helpers` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.tdlib_observer` | `app.modules.neuro_commenting.tdlib_observer` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.neuro_commenting.tdlib_runtime` | `app.modules.neuro_commenting.tdlib_runtime` | Preserves old neuro-commenting service imports while implementation is module-owned. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.retention_worker` | `app.modules.account_lifecycle.retention` | Preserves old retention worker imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.risk_gate` | `app.modules.account_safety.action_gate` | Preserves old account action gate service imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.safety_gate_cache` | `app.modules.account_safety.cache` | Preserves old account safety gate cache imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.safety_gate_reserve` | `app.modules.account_safety.reserve` | Preserves old account safety gate reservation imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.warmup` | `app.modules.warmup.service` | Preserves old warmup service imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.warmup_dispatch` | `app.modules.warmup.dispatcher` | Preserves old warmup dispatch service imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.warmup_isolation` | `app.modules.warmup.isolation` | Preserves old warmup isolation imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.warmup_p2p` | `app.modules.warmup.p2p` | Preserves old warmup peer selection imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.warmup_readiness` | `app.modules.warmup.readiness` | Preserves old warmup readiness imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.warmup_worker` | `app.modules.warmup.worker` | Preserves old warmup due-session worker service imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.workspaces` | `app.workspace_bootstrap` | Preserves old local default workspace bootstrap service imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.services.workspace_safety_policy` | `app.modules.account_safety.policy` | Preserves old workspace safety policy service imports. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.workers.account_update_jobs` | `app.modules.account_editing.executor` | Preserves old RQ worker import path. | `tests, external_compatibility` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.workers.warmup_dispatch_jobs` | `app.modules.warmup.jobs` | Preserves old warmup dispatch worker import path. | `tests, external_compatibility, workflow_registry` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |
| `app.workers.warmup_jobs` | `app.modules.warmup.jobs` | Preserves old warmup due-session worker import path. | `tests, external_compatibility, workflow_registry` | New behavior or canonical implementation changes. | Remove after internal call-sites, tests, and external import risk are cleared. | Tests and external compatibility imports until reference audit clears the path. |

Deprecation process:

- `docs/architecture/legacy-wrapper-deprecation-plan.md` defines the staged removal policy.
- `docs/architecture/legacy-wrappers.json` is the deterministic machine-readable wrapper manifest.
- `backend/scripts/legacy_wrapper_audit.py` validates the committed manifest; `--print` emits the deterministic manifest to stdout.

All wrappers remain in Stage 0 compatibility active. This audit does not remove
wrappers, emit runtime deprecation warnings, rename paths, or change exports.
Architecture tests enforce that feature modules do not import these wrappers and
that wrapper files keep compatibility-owner docstrings.
