services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: stylisttg
      POSTGRES_USER: stylisttg
      POSTGRES_PASSWORD: stylisttg
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:8
    ports:
      - "6379:6379"

volumes:
  postgres_data:
