# Legacy Handoff Pointer

Current project memory lives in `.mex/`.

Use this startup path:

1. `AGENTS.md`
2. `.mex/ROUTER.md`
3. `.mex/status/current.md`
4. Routed files under `.mex/context/` and `.mex/patterns/`

This file is only a compatibility pointer for old bookmarks. Older long-form handoff content should be read from Git history only when current `.mex` memory and `docs/` do not answer the question.
