# Legacy Handoff Pointer

Current project memory lives in `.mex`.

Use this startup path:

1. `AGENTS.md`
2. `.mex/ROUTER.md`
3. `.mex/status/current.md`
4. Routed files under `.mex/context/` and `.mex/patterns/`

This file is only a compatibility pointer for old bookmarks.

Historical handoff context starts at `docs/archive/agent-handoff-2026-05.md`. Read it only when current `.mex` memory, current `docs/`, and source code do not answer the question.
